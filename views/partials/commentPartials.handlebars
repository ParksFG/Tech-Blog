{{!-- <div class="commentContainer">
  <input type="hidden" name="post-id" value="{{id}}" />
  <div>
    <h4>
      <span class="ms-2">Comment: User: {{user.userName}}</span>
    </h4>
    <div>Posted on: {{formatDate date}}
    </div>
  </div>
  <div>Comment: {{content}}</div>
</div> --}}

<div class="commentContainer">
  <input type="hidden" name="post-id" value="{{id}}"/>
  <h3>Comment:</h3>
  <div class="commentContent">
    <p>{{content}}</p>
    <br>
    <p>-{{user.userName}}, {{formatDate date}}</p>
  </div>
</div>
